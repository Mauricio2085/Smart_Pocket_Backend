version: '3.3'

services:
  postgres:
    image: postgres:14
    environment:
      - POSTGRES_DB=smart_pocket
      - POSTGRES_USER=mauricio
      - POSTGRES_PASSWORD=M4ur1c100c4mp0
    ports:
      - 5432:5432
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=maoca2085@gmail.com
      - PGADMIN_DEFAULT_PASSWORD=root
    ports:
      - 5050:80
